Roblox Avatar Fix v1.0.0 - TEST BUILD

Changes in this test build:
- No run-as-admin.vbs launcher.
- service.bat uses Windows PowerShell Start-Process -Verb RunAs for elevation.
- Missing list-exclude-user.txt is automatically recreated and repaired.
- The project does not require zapret to run.

Test:
1. Run service.bat as normal user.
2. Accept the UAC prompt.
3. Check that the menu appears.
4. Delete lists\list-exclude-user.txt and run service.bat again.
5. Confirm the file is recreated and the status does not remain NO LIST FILE.
